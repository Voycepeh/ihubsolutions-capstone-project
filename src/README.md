# Source code

Use this folder for reusable Python code shared across notebooks, analysis steps or deployment components.

Examples include data-loading helpers, feature engineering functions, evaluation utilities and common transformations.

Keep one-off exploration in `notebooks/` until the logic is stable enough to reuse.
