# Notebooks

Use this folder for exploratory data analysis, data preparation, modelling, evaluation and project experiments.

Keep notebooks understandable to teammates. Prefer clear names, short explanatory Markdown cells and reproducible paths that read local data from `data/`.

Move logic that becomes broadly reusable into `src/` rather than duplicating it across many notebooks.
